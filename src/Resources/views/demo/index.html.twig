<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>GA4 Analytics Data Bundle Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 20px;
            padding-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row mb-4">
            <div class="col">
                <h1>GA4 Analytics Data Bundle Demo</h1>
                <p class="lead">This is a demo page for the GA4 Analytics Data Bundle</p>
            </div>
        </div>

        <div class="row mb-4">
            <div class="col">
                <div class="card">
                    <div class="card-header">
                        Configuration Status
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">Registered Analytics Clients</h5>
                        {% if clients|length > 0 %}
                            <ul>
                                {% for client in clients %}
                                    <li>{{ client }}</li>
                                {% endfor %}
                            </ul>
                            
                            {% if hasDefaultClient %}
                                <div class="alert alert-success">
                                    Default client is configured. You can view the <a href="{{ path('demo_dashboard') }}" class="alert-link">dashboard</a>.
                                </div>
                            {% else %}
                                <div class="alert alert-warning">
                                    No default client configured. Please set the default_client in your configuration.
                                </div>
                            {% endif %}
                        {% else %}
                            <div class="alert alert-danger">
                                No clients registered. Please check your configuration.
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-header">
                        Documentation
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">Configuration</h5>
                        <p>Make sure your environment variables are set:</p>
                        <ul>
                            <li><code>ANALYTICS_PROPERTY_ID</code>: Your GA4 property ID</li>
                            <li><code>ANALYTICS_CREDENTIALS_PATH</code>: Path to your service account credentials JSON file</li>
                        </ul>
                        
                        <h5 class="card-title mt-4">Available Pages</h5>
                        <ul>
                            <li><a href="{{ path('demo_index') }}">Home</a> - This page</li>
                            <li><a href="{{ path('demo_dashboard') }}">Dashboard</a> - Example analytics dashboard</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>